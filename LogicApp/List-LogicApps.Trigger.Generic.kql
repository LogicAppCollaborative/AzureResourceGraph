resources
| where type == "microsoft.logic/workflows"
| project name, resourceGroup, subscriptionId, state=properties.state, triggerName=bag_keys(properties.definition.triggers)[0],triggerInfo=properties.definition.triggers, properties
